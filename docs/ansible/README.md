# docs/ansible/

Ansible usage and reference — day-to-day operation, not build procedures (those live in
`buildsheets/` and `bootstrap/`).

See [../INDEX.md](../INDEX.md) for the full file list and descriptions.
