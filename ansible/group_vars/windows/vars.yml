---
# =============================================================================
# group_vars/windows/vars.yml
# Example Music Limited — Variables for all Windows hosts
# =============================================================================
# Changelog:
#   2026-06-20  Initial file
#   2026-07-06  Added rustdesk, wget, busybox (all previously binaries
#               pulled from the asset server, which doesn't host them);
#               removed dua-cli (now tasks/dua_cli.yml, an upstream GitHub
#               release, not a Chocolatey package) and emptied
#               binaries_common (dua.exe/wget.exe both migrated away).
#   2026-07-06  Removed choco_packages_common entirely — it was dead code
#               (shadowed by both real consumers' own play vars:, see
#               note below) and had drifted from them besides. Not worth
#               a confusing third copy.
# =============================================================================

# NOTE: choco_packages_common is NOT defined here. Both consumers
# (playbooks/05-bootstrap.yml and playbooks/40-choco-packages.yml) run
# against a bare-IP host with no inventory group membership yet, so they
# each keep their own self-contained, byte-for-byte-identical copy of this
# list instead — see either file's design note. A copy used to live here
# too, but nothing ever read it (play vars: always wins over group_vars,
# so it was silently shadowed) and it had drifted from the two real
# copies; removed rather than leave a third, misleading version around.

# Binaries deployed to C:\Windows\ on every host (arch-aware, src resolved at runtime)
# Format: { src: "<filename in arch folder>", dest: "<filename on host>" }
# dua.exe and wget.exe removed — dua-cli now comes from tasks/dua_cli.yml
# (upstream GitHub release, arch-aware), wget is a Chocolatey package
# (choco_packages_common above). Neither was actually hosted by the asset
# server in the first place.
binaries_common: []

# Registry keys applied to every Windows host
registry_common:
  - { path: 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System',              name: EnableLUA,                    data: 1,  type: dword }
  - { path: 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System',              name: ConsentPromptBehaviorAdmin,   data: 2,  type: dword }
  - { path: 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System',              name: InactivityTimeoutSecs,        data: "{{ exa_lock_timeout_secs }}", type: dword }
  - { path: 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System',              name: AutoAdminLogon,               data: "0", type: string }
  - { path: 'HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server',                       name: fDenyTSConnections,           data: 0,  type: dword }
  - { path: 'HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp',  name: UserAuthentication,           data: 1,  type: dword }
  - { path: 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\WindowsUpdate\AU',                  name: NoAutoUpdate,                 data: 1,  type: dword }
  - { path: 'HKLM:\SYSTEM\CurrentControlSet\Services\LanmanServer\Parameters',             name: SMB1,                         data: 0,  type: dword }
  - { path: 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced',           name: HideFileExt,                  data: 0,  type: dword }
  - { path: 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced',           name: Hidden,                       data: 1,  type: dword }
  - { path: 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Themes\Personalize',          name: AppsUseLightTheme,            data: 0,  type: dword }
  - { path: 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Themes\Personalize',          name: SystemUsesLightTheme,         data: 0,  type: dword }

# Registry keys applied when exa_deadvertise_enabled is true (desktop + laptop only)
registry_deadvertise:
  - { path: 'HKCU:\Software\Policies\Microsoft\Windows\Explorer',                              name: DisableSearchBoxSuggestions,           data: 1,  type: dword }
  - { path: 'HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\ContentDeliveryManager',          name: SystemPaneSuggestionsEnabled,          data: 0,  type: dword }
  - { path: 'HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\ContentDeliveryManager',          name: SubscribedContent-338388Enabled,       data: 0,  type: dword }
  - { path: 'HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\ContentDeliveryManager',          name: SubscribedContent-338389Enabled,       data: 0,  type: dword }
  - { path: 'HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\ContentDeliveryManager',          name: SoftLandingEnabled,                    data: 0,  type: dword }
  - { path: 'HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced',               name: Start_TrackProgs,                      data: 0,  type: dword }
  - { path: 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\CloudContent',                         name: DisableWindowsConsumerFeatures,        data: 1,  type: dword }
  - { path: 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DataCollection',                       name: AllowTelemetry,                        data: 0,  type: dword }
  - { path: 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DataCollection',                       name: DisableOneSettingsDownloads,           data: 1,  type: dword }
