# =============================================================================
# configs/inventory/meshcentral.ini
# Example Music Limited — MeshCentral server inventory
# =============================================================================
# Remote management platform, phase 1/2 (see project notes). Single instance,
# CLD LAN only -- same "one central management box" pattern as rudder.ini/
# salt.ini.
#
# RETIRED 2026-08-08 -- EXAMSHCLD001 decommissioned, TacticalRMM's own
# bundled MeshCentral is a confirmed real-use replacement (Robert, live
# 2026-08-07/08). Host line commented out rather than deleted -- keeps the
# historical hostname/IP (192.168.69.13, now free for reuse) discoverable.
# See playbooks/tacticalrmm/README.md's "EXAMSHCLD001 — RETIRED" section.

# --- MeshCentral server (retired) ---------------------------------------------
[meshcentral_servers]
# EXAMSHCLD001  ansible_host=192.168.69.13  ansible_connection=ssh  # RETIRED 2026-08-08 -- see header above

[meshcentral:children]
meshcentral_servers
