# =============================================================================
# configs/inventory/meshcentral.ini
# Example Music Limited — MeshCentral server inventory
# =============================================================================
# Remote management platform, phase 1/2 (see project notes). Single instance,
# CLD LAN only -- same "one central management box" pattern as rudder.ini/
# salt.ini.

# --- MeshCentral server -------------------------------------------------------
[meshcentral_servers]
EXAMSHCLD001  ansible_host=192.168.69.13  ansible_connection=ssh  # MeshCentral server (CLD LAN)

[meshcentral:children]
meshcentral_servers
